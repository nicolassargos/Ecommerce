@model int

@*@Html.DropDownList("id", (IEnumerable<SelectListItem>)ViewBag.categories , new { @class = "" })*@

@*Html.DropDownListFor(x => x, ViewBag.Categories as SelectListItem));*@
    
    @*Model, (IEnumerable<SelectListItem>)ViewBag.categories);*@

